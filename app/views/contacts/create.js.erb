<% if @contact.errors.any? %>
 alert('Invalid Details');
<% else %>
 $("#contactsTable").prepend("<%= escape_javascript render('contact_row', contact: @contact) %>");
 $(".close-modal").click();
<% end %>